# leave as localhost if working on local machine
host = "localhost"
# your mysql user
# make sure the user has all grants
user = "root"
# mysql password for user, leave blank if password doesn't exists
password = ""
# mysql database, where the sql file is imported
database = "python"